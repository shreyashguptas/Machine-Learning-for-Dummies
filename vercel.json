{
  "buildCommand": "python -m pip install -r requirements-book.yml --no-cache-dir && jupyter-book build . --builder html",
  "outputDirectory": "_build/html",
  "installCommand": "python -m pip install --no-cache-dir jupyter-book",
  "framework": null,
  "builds": [
    {
      "src": "**",
      "use": "@vercel/python"
    }
  ]
} 