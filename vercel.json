{
    "version": 2,
    "buildCommand": "export PATH=\"/vercel/.local/bin:$PATH\" && pip install --user jupyter-book && jupyter-book build .",
    "installCommand": "python3 -m ensurepip && export PATH=\"/vercel/.local/bin:$PATH\" && pip install --user --upgrade pip",
    "outputDirectory": "_build/html"
}
