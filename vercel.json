{
  "buildCommand": "python scripts/convert_notebooks.py && jekyll build",
  "outputDirectory": "_site",
  "framework": "jekyll",
  "installCommand": "pip install nbconvert nbformat && bundle install"
} 