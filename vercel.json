{
    "buildCommand": "python -m pip install -r requirements.txt && jupyter-book build . --all",
    "outputDirectory": "_build/html",
    "installCommand": "python -m ensurepip && python -m pip install --upgrade pip"
}